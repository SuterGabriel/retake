# Learning log

One entry per corrected mental model or notable failure. Not a diary. Use `/learning-log` in Claude Code to draft one from a debugging session, then edit it yourself.

`python-for-ts-devs.md` grows from `/explain-python` and becomes a blog post at the end.
